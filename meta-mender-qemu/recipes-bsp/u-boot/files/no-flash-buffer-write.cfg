# qemu commit 284a7ee2e290 broke buffered writes to the emulated NOR
# flash, which U-Boot uses when saving the environment. It shipped in
# qemu 9.0 and was backported to 8.2.1, 8.1.5 and 7.2.9.
#
# NB: the next line is NOT a comment -- "# CONFIG_x is not set" is Kconfig's
# syntax for forcing an option off in a config fragment.
# CONFIG_SYS_FLASH_USE_BUFFER_WRITE is not set
